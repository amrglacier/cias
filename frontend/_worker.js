// CIAS Frontend - Pages Worker (Proxy)
// Routes /api/* requests to Supabase REST API directly
// This bypasses the GFW-blocked workers.dev domain
// Auto-generated by scripts/build-worker.js - DO NOT EDIT MANUALLY

const SB_URL = "https://snycievdfcyoytthxspm.supabase.co";
const SB_KEY = ""; // Set via Cloudflare Pages environment variable: SUPABASE_SERVICE_KEY

const CORS_HEADERS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
};

function jsonResponse(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { "Content-Type": "application/json", ...CORS_HEADERS },
  });
}

async function supabaseGet(table, query, select = "*") {
  const url = new URL(SB_URL + "/rest/v1/" + table);
  url.searchParams.set("select", select);
  for (const [key, value] of Object.entries(query)) {
    url.searchParams.set(key, value);
  }
  const resp = await fetch(url.toString(), {
    headers: {
      apikey: SB_KEY,
      Authorization: "Bearer " + SB_KEY,
    },
  });
  return resp.json();
}

async function supabasePost(table, body) {
  const resp = await fetch(SB_URL + "/rest/v1/" + table, {
    method: "POST",
    headers: {
      apikey: SB_KEY,
      Authorization: "Bearer " + SB_KEY,
      "Content-Type": "application/json",
      Prefer: "return=representation,resolution=merge-duplicates",
    },
    body: JSON.stringify(body),
  });
  return resp.json();
}

function getHTML() {
  return `<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="theme-color" content="#0b0f17">
  <title>CIAS - 足球预测系统</title>
  <style>
    :root{
      --bg:#0b0f17;
      --bg-card:#151b28;
      --bg-card-hover:#1a2233;
      --bg-input:#0d1320;
      --border:#1e293b;
      --border-light:#2a3a52;
      --text:#e8edf5;
      --text-dim:#8493a8;
      --text-muted:#5a6a80;
      --accent:#10b981;
      --accent-light:#34d399;
      --accent-glow:rgba(16,185,129,.15);
      --accent2:#6366f1;
      --accent2-light:#818cf8;
      --warning:#f59e0b;
      --danger:#ef4444;
      --success:#22c55e;
      --info:#3b82f6;
      --radius:14px;
      --rs:10px;
      --sb:env(safe-area-inset-bottom,0px);
      --st:env(safe-area-inset-top,0px);
      --gradient-primary:linear-gradient(135deg,#10b981 0%,#059669 100%);
      --gradient-secondary:linear-gradient(135deg,#6366f1 0%,#4f46e5 100%);
      --gradient-bg:radial-gradient(ellipse at top,rgba(16,185,129,.04) 0%,transparent 60%);
      --shadow-sm:0 1px 3px rgba(0,0,0,.3);
      --shadow-md:0 4px 12px rgba(0,0,0,.4);
      --shadow-lg:0 8px 24px rgba(0,0,0,.5);
    }
    *{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
    html{-webkit-text-size-adjust:100%}
    body{
      font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',system-ui,sans-serif;
      background:var(--bg);
      background-image:var(--gradient-bg);
      background-attachment:fixed;
      color:var(--text);
      min-height:100vh;
      min-height:100dvh;
      overflow-x:hidden;
      padding-bottom:calc(68px + var(--sb));
      -webkit-font-smoothing:antialiased;
    }
    ::-webkit-scrollbar{width:3px;height:3px}
    ::-webkit-scrollbar-track{background:transparent}
    ::-webkit-scrollbar-thumb{background:var(--border-light);border-radius:2px}

    /* Header */
    .hdr{
      position:sticky;top:0;z-index:100;
      background:rgba(11,15,23,.82);
      backdrop-filter:blur(24px) saturate(180%);
      -webkit-backdrop-filter:blur(24px) saturate(180%);
      border-bottom:1px solid var(--border);
      padding:calc(10px + var(--st)) 16px 10px;
    }
    .hdr-in{display:flex;align-items:center;justify-content:space-between;max-width:680px;margin:0 auto}
    .logo{display:flex;align-items:center;gap:10px}
    .logo-ic{
      width:36px;height:36px;border-radius:10px;
      background:var(--gradient-primary);
      display:flex;align-items:center;justify-content:center;
      font-size:18px;font-weight:800;color:#fff;flex-shrink:0;
      box-shadow:0 2px 8px rgba(16,185,129,.3);
    }
    .logo-t{font-size:17px;font-weight:800;letter-spacing:-.5px}
    .logo-s{font-size:9px;color:var(--text-muted);font-weight:600;letter-spacing:.5px;text-transform:uppercase}
    .hlt{display:flex;align-items:center;gap:6px;font-size:11px;color:var(--text-dim);font-weight:600;padding:4px 10px;background:var(--bg-card);border-radius:20px;border:1px solid var(--border)}
    .hlt .dt{width:7px;height:7px;border-radius:50%;background:var(--success);box-shadow:0 0 8px rgba(34,197,94,.6);animation:pulse 2s infinite}
    .hlt.off .dt{background:var(--danger);box-shadow:0 0 8px rgba(239,68,68,.6)}
    @keyframes pulse{0%,100%{opacity:1}50%{opacity:.5}}

    /* Main */
    .main{max-width:680px;margin:0 auto;padding:16px}
    .tc{display:none}
    .tc.on{display:block;animation:fi .25s cubic-bezier(.16,1,.3,1)}
    @keyframes fi{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}

    /* Search bar */
    .sb-bar{display:flex;gap:8px;margin-bottom:16px}
    .sb-bar input{
      flex:1;background:var(--bg-card);border:1px solid var(--border);
      color:var(--text);padding:12px 16px;border-radius:var(--rs);
      font-size:14px;outline:none;min-width:0;transition:border-color .2s,box-shadow .2s;
    }
    .sb-bar input:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-glow)}
    .sb-bar input::placeholder{color:var(--text-muted)}

    /* Buttons */
    .btn{
      background:var(--gradient-primary);color:#fff;border:none;
      padding:12px 18px;border-radius:var(--rs);font-size:13px;font-weight:700;
      cursor:pointer;white-space:nowrap;display:inline-flex;align-items:center;gap:5px;
      transition:transform .1s,opacity .2s;box-shadow:0 2px 8px rgba(16,185,129,.2);
    }
    .btn:active{transform:scale(.95)}
    .btn2{background:var(--bg-card);color:var(--accent-light);border:1px solid var(--border-light);box-shadow:none}
    .btn-s{padding:8px 14px;font-size:12px}

    /* Cards */
    .card{
      background:var(--bg-card);border:1px solid var(--border);
      border-radius:var(--radius);padding:18px;margin-bottom:12px;
      transition:border-color .2s;
    }
    .ct{
      font-size:13px;font-weight:800;color:var(--accent-light);
      margin-bottom:14px;display:flex;align-items:center;gap:6px;
      text-transform:uppercase;letter-spacing:.5px;
    }

    /* Prediction card */
    .pc{
      background:var(--bg-card);border:1px solid var(--border);
      border-radius:var(--radius);padding:18px;margin-bottom:12px;
      position:relative;overflow:hidden;
      transition:transform .1s,border-color .2s;
    }
    .pc:active{transform:scale(.99)}
    .pc.lk{border-color:rgba(16,185,129,.25)}
    .pc.lk::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:var(--gradient-primary)}
    .pc.fd{border-color:rgba(245,158,11,.25)}
    .pc.fd::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:linear-gradient(90deg,#f59e0b,#f97316)}

    .ph{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px}
    .pm{font-size:14px;font-weight:700;letter-spacing:-.3px}
    .pv{font-size:10px;font-weight:800;padding:3px 8px;border-radius:6px;text-transform:uppercase;letter-spacing:.5px}
    .vINITIAL{background:rgba(99,102,241,.15);color:var(--accent2-light)}
    .vPERIODIC{background:rgba(59,130,246,.15);color:#60a5fa}
    .vFUSE{background:rgba(245,158,11,.15);color:#fbbf24}
    .vREVIEW{background:rgba(100,116,139,.15);color:#94a3b8}

    .pd{
      font-size:13px;padding:12px 14px;background:var(--bg-input);
      border-radius:var(--rs);margin:8px 0;border-left:3px solid var(--accent);
      line-height:1.6;color:var(--text);
    }

    /* Score boxes */
    .ps{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:10px 0}
    .sbox{
      background:var(--bg-input);border-radius:var(--rs);
      padding:12px 14px;position:relative;overflow:hidden;
    }
    .sl{font-size:10px;color:var(--text-dim);margin-bottom:4px;font-weight:600;text-transform:uppercase;letter-spacing:.3px}
    .sv{font-size:22px;font-weight:800;letter-spacing:-.5px}
    .sv .ht{font-size:11px;color:var(--text-muted);font-weight:500}

    /* Probability bar */
    .prob-bar{
      display:flex;height:6px;border-radius:3px;overflow:hidden;margin:10px 0;
      background:var(--bg-input);
    }
    .prob-h{background:var(--accent);transition:width .5s ease}
    .prob-d{background:var(--text-muted);transition:width .5s ease}
    .prob-a{background:var(--accent2);transition:width .5s ease}
    .prob-labels{display:flex;justify-content:space-between;font-size:10px;color:var(--text-dim);margin-top:4px;font-weight:600}

    /* Alignment badges */
    .pf{display:flex;justify-content:space-between;align-items:center;font-size:11px;color:var(--text-dim);margin-top:8px}
    .bdg{padding:3px 8px;border-radius:6px;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.5px}
    .ba{background:rgba(34,197,94,.12);color:#4ade80}
    .bf{background:rgba(239,68,68,.12);color:#f87171}
    .bp{background:rgba(100,116,139,.12);color:#94a3b8}

    /* Delta explanation */
    .dx{
      font-size:12px;color:#fcd34d;padding:10px 14px;
      background:rgba(245,158,11,.06);border-radius:var(--rs);
      margin-top:8px;border-left:2px solid var(--warning);line-height:1.5;
    }

    /* Factor list */
    details summary{cursor:pointer;font-size:11px;color:var(--text-dim);margin-top:10px;user-select:none;font-weight:600}
    details summary::-webkit-details-marker{display:none}
    details summary::before{content:'\\25B6\\00a0';transition:transform .2s;display:inline-block}
    details[open] summary::before{content:'\\25BC\\00a0'}
    .fl{list-style:none;padding:0;margin-top:8px}
    .fi2{display:flex;justify-content:space-between;padding:7px 0;border-bottom:1px solid var(--border);font-size:12px}
    .fi2:last-child{border-bottom:none}
    .fid{color:var(--text-dim);font-weight:600}
    .fval{color:var(--accent-light);font-family:'SF Mono','Fira Code',monospace;font-weight:600}

    /* Stats grid */
    .sg{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:16px}
    .sbox2{
      background:var(--bg-card);border:1px solid var(--border);
      border-radius:var(--radius);padding:18px 8px;text-align:center;
      position:relative;overflow:hidden;
    }
    .sbox2::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:var(--gradient-primary);opacity:.6}
    .sv2{font-size:28px;font-weight:800;color:var(--accent-light);line-height:1;letter-spacing:-1px}
    .sl2{font-size:10px;color:var(--text-dim);margin-top:6px;font-weight:600;text-transform:uppercase;letter-spacing:.5px}

    /* Odds table */
    .dt2{width:100%;border-collapse:collapse;margin-top:8px}
    .dt2 th,.dt2 td{padding:9px 6px;text-align:center;border-bottom:1px solid var(--border);font-size:12px}
    .dt2 th{color:var(--text-dim);font-weight:700;font-size:10px;text-transform:uppercase;letter-spacing:.3px}
    .ts{color:var(--warning);font-weight:800}
    .tm{color:var(--danger);font-weight:800}

    /* Fixture card */
    .fc{
      background:var(--bg-card);border:1px solid var(--border);
      border-radius:var(--radius);padding:16px;margin-bottom:10px;
      transition:border-color .2s;
    }
    .ft{display:flex;justify-content:space-between;align-items:flex-start;gap:8px}
    .fte{font-size:15px;font-weight:700;line-height:1.4;letter-spacing:-.3px}
    .ftl{font-size:11px;color:var(--text-muted);margin-top:3px;font-weight:500}
    .fti{font-size:12px;color:var(--accent-light);white-space:nowrap;text-align:right;font-weight:600}
    .fm{font-size:11px;color:var(--text-dim);margin-top:8px;line-height:1.5}

    /* Config form */
    .cf{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}
    .cf-full{grid-column:1/-1}
    .cf label{font-size:11px;color:var(--text-dim);display:block;margin-bottom:4px;font-weight:600;text-transform:uppercase;letter-spacing:.3px}
    .cf input{
      width:100%;background:var(--bg-input);border:1px solid var(--border);
      color:var(--text);padding:11px 14px;border-radius:var(--rs);
      font-size:15px;outline:none;transition:border-color .2s,box-shadow .2s;
    }
    .cf input:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-glow)}
    .cs{font-size:12px;color:var(--text-dim);margin-top:10px;line-height:1.6;padding:8px 12px;background:var(--bg-input);border-radius:var(--rs)}

    /* Toggle switch */
    .toggle-row{display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid var(--border)}
    .toggle-row:last-child{border-bottom:none}
    .toggle-label{font-size:13px;font-weight:600}
    .toggle-desc{font-size:11px;color:var(--text-dim);margin-top:2px}

    /* Empty & loading states */
    .es{text-align:center;padding:60px 20px;color:var(--text-muted)}
    .esi{font-size:40px;margin-bottom:12px;opacity:.4}
    .est{font-size:14px;line-height:1.6}
    .ls{text-align:center;padding:40px 20px;color:var(--accent-light)}
    .sp{
      width:24px;height:24px;
      border:2.5px solid var(--border);
      border-top-color:var(--accent);
      border-radius:50%;
      animation:spin .7s linear infinite;
      margin:0 auto 12px;
    }
    @keyframes spin{to{transform:rotate(360deg)}}

    /* Bottom nav */
    .bnav{
      position:fixed;bottom:0;left:0;right:0;
      background:rgba(11,15,23,.92);
      backdrop-filter:blur(24px) saturate(180%);
      -webkit-backdrop-filter:blur(24px) saturate(180%);
      border-top:1px solid var(--border);
      display:flex;justify-content:space-around;
      padding:6px 0 calc(6px + var(--sb));z-index:100;
    }
    .ni{
      flex:1;display:flex;flex-direction:column;align-items:center;gap:2px;
      padding:7px 2px;cursor:pointer;color:var(--text-muted);
      transition:color .2s;position:relative;
    }
    .ni.on{color:var(--accent-light)}
    .ni.on::before{content:'';position:absolute;top:0;width:24px;height:2px;background:var(--accent);border-radius:0 0 3px 3px}
    .ni svg{width:22px;height:22px;transition:transform .2s}
    .ni.on svg{transform:scale(1.1)}
    .ni span{font-size:10px;font-weight:700}

    /* Responsive */
    @media(min-width:640px){
      .main{padding:24px}
      .sg{gap:14px}
      .sv2{font-size:32px}
      .sv{font-size:26px}
    }
  </style>
</head>
<body>
  <header class="hdr">
    <div class="hdr-in">
      <div class="logo">
        <div class="logo-ic">C</div>
        <div>
          <div class="logo-t">CIAS</div>
          <div class="logo-s">Football Prediction</div>
        </div>
      </div>
      <div id="health-status" class="hlt">
        <div class="dt"></div><span>Loading</span>
      </div>
    </div>
  </header>

  <main class="main">
    <!-- Tab: Predictions -->
    <div id="tab-predictions" class="tc on">
      <div class="sb-bar">
        <input type="text" id="match-id-input" placeholder="输入 Match ID...">
        <button class="btn btn-s" onclick="loadPrediction()">查询</button>
        <button class="btn btn2 btn-s" onclick="loadAllPredictions()">全部</button>
      </div>
      <div id="predictions-container">
        <div class="es">
          <div class="esi">&#9917;</div>
          <div class="est">点击「全部」查看所有预测<br>或输入 Match ID 查询</div>
        </div>
      </div>
    </div>

    <!-- Tab: Review -->
    <div id="tab-review" class="tc">
      <div class="sg">
        <div class="sbox2"><div class="sv2" id="stat-total">-</div><div class="sl2">总复盘</div></div>
        <div class="sbox2"><div class="sv2" id="stat-hitrate">-</div><div class="sl2">命中率</div></div>
        <div class="sbox2"><div class="sv2" id="stat-upsets">-</div><div class="sl2">冷门</div></div>
      </div>
      <div id="reviews-container">
        <div class="ls"><div class="sp"></div>加载中...</div>
      </div>
    </div>

    <!-- Tab: Fixtures -->
    <div id="tab-fixtures" class="tc">
      <div id="fixtures-container">
        <div class="ls"><div class="sp"></div>加载中...</div>
      </div>
    </div>

    <!-- Tab: Match Facts -->
    <div id="tab-matches" class="tc">
      <div id="matches-container">
        <div class="ls"><div class="sp"></div>加载中...</div>
      </div>
    </div>

    <!-- Tab: Config -->
    <div id="tab-config" class="tc">
      <div class="card">
        <div class="ct">&#9201; 购彩时间配置</div>
        <div class="cf">
          <div>
            <label>开始 (赛前N小时)</label>
            <input type="number" id="bw-start" min="1" max="72" value="2">
          </div>
          <div>
            <label>截止 (赛前N分钟)</label>
            <input type="number" id="bw-end" min="0" max="120" value="15">
          </div>
          <div>
            <label>基本面延迟 (购彩开始后N小时)</label>
            <input type="number" id="bw-fund-delay" min="0" max="6" step="0.5" value="0.5">
          </div>
          <div>
            <label>终盘锁定 (购彩结束前N分钟)</label>
            <input type="number" id="bw-final-lock" min="1" max="60" value="15">
          </div>
          <div class="cf-full">
            <label>赛季</label>
            <input type="text" id="bw-season" value="2025">
          </div>
        </div>
        <button class="btn btn-s" onclick="saveBettingWindowConfig()">保存配置</button>
        <div id="bw-status" class="cs"></div>
      </div>
      <div class="card">
        <div class="ct">&#9878; 因子权重</div>
        <div id="config-container">
          <div class="ls"><div class="sp"></div>加载中...</div>
        </div>
      </div>
      <div class="card">
        <div class="ct">&#128202; 复盘配置</div>
        <div id="review-config-container">
          <div class="ls"><div class="sp"></div>加载中...</div>
        </div>
      </div>
    </div>
  </main>

  <nav class="bnav">
    <div class="ni on" data-tab="predictions" onclick="switchTab('predictions')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
      <span>预测</span>
    </div>
    <div class="ni" data-tab="review" onclick="switchTab('review')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="M18 17V9M13 17V5M8 17v-3"/></svg>
      <span>复盘</span>
    </div>
    <div class="ni" data-tab="fixtures" onclick="switchTab('fixtures')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
      <span>赛程</span>
    </div>
    <div class="ni" data-tab="matches" onclick="switchTab('matches')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="8" y1="13" x2="16" y2="13"/><line x1="8" y1="17" x2="16" y2="17"/></svg>
      <span>基本面</span>
    </div>
    <div class="ni" data-tab="config" onclick="switchTab('config')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
      <span>设置</span>
    </div>
  </nav>

  <script>
    var API_BASE='';
    function switchTab(n){
      document.querySelectorAll('.ni').forEach(function(x){x.classList.remove('on')});
      document.querySelector('.ni[data-tab="'+n+'"]').classList.add('on');
      document.querySelectorAll('.tc').forEach(function(x){x.classList.remove('on')});
      document.getElementById('tab-'+n).classList.add('on');
      if(n==='matches'&&!document.getElementById('matches-container').dataset.loaded){loadAllMatchFacts()}
      if(n==='fixtures'&&!document.getElementById('fixtures-container').dataset.loaded){loadUpcomingMatches()}
      if(n==='config'&&!document.getElementById('bw-status').dataset.loaded){loadBettingWindowConfig();loadConfig()}
      if(n==='review'&&!document.getElementById('reviews-container').dataset.loaded){loadReviews()}
      window.scrollTo(0,0);
    }

    async function checkHealth(){
      try{
        var r=await fetch(API_BASE+'/api/health');var d=await r.json();var el=document.getElementById('health-status');
        if(d.status==='healthy'){el.className='hlt';el.innerHTML='<div class="dt"></div><span>Online</span>'}
        else{el.className='hlt off';el.innerHTML='<div class="dt"></div><span>Error</span>'}
      }catch(e){
        var el=document.getElementById('health-status');el.className='hlt off';el.innerHTML='<div class="dt"></div><span>Offline</span>';
      }
    }

    async function loadPrediction(){
      var mid=document.getElementById('match-id-input').value.trim();
      if(!mid){alert('请输入 Match ID');return}
      document.getElementById('predictions-container').innerHTML='<div class="ls"><div class="sp"></div>查询中...</div>';
      try{
        var r=await fetch(API_BASE+'/api/prediction?matchId='+encodeURIComponent(mid));
        if(!r.ok){document.getElementById('predictions-container').innerHTML='<div class="es"><div class="esi">&#128269;</div><div class="est">未找到 '+mid+' 的锁定预测</div></div>';return}
        var d=await r.json();
        if(!d.prediction){document.getElementById('predictions-container').innerHTML='<div class="es"><div class="esi">&#128269;</div><div class="est">未找到 '+mid+' 的预测结果</div></div>';return}
        renderPredictions([d.prediction]);
        try{var or=await fetch(API_BASE+'/api/odds-snapshots?matchId='+encodeURIComponent(mid));var od=await or.json();if(od.snapshots&&od.snapshots.length>0){renderOdds(od.snapshots)}}catch(e){}
      }catch(e){document.getElementById('predictions-container').innerHTML='<div class="es"><div class="est">查询失败: '+e.message+'</div></div>'}
    }

    async function loadAllPredictions(){
      document.getElementById('predictions-container').innerHTML='<div class="ls"><div class="sp"></div>加载中...</div>';
      try{
        var r=await fetch(API_BASE+'/api/all-predictions');var d=await r.json();
        if(!d.predictions||d.predictions.length===0){document.getElementById('predictions-container').innerHTML='<div class="es"><div class="esi">&#9917;</div><div class="est">暂无预测数据</div></div>';return}
        renderPredictions(d.predictions);
      }catch(e){document.getElementById('predictions-container').innerHTML='<div class="es"><div class="est">加载失败: '+e.message+'</div></div>'}
    }

    function renderPredictions(ps){
      if(!ps||ps.length===0){document.getElementById('predictions-container').innerHTML='<div class="es"><div class="est">无预测数据</div></div>';return}
      var h=ps.filter(function(p){return p}).map(function(p){
        var lk=p.is_lock?'lk':'',fd=p.alignment_forced_degrade?'fd':'',vc='v'+p.version_tag;
        var ab=p.alignment_status==='aligned'?'<span class="bdg ba">ALIGNED</span>':p.alignment_forced_degrade?'<span class="bdg bf">FORCED</span>':'<span class="bdg bp">PENDING</span>';
        var pt=p.primary_result==='home_win'?'主胜':p.primary_result==='draw'?'平局':'客胜';
        var ht=p.hedge_result?(p.hedge_result==='home_win'?'主胜':p.hedge_result==='draw'?'平局':'客胜'):'无';
        var fh=Object.entries(p.key_factors||{}).map(function(r){var id=r[0],f=r[1];return '<div class="fi2"><span class="fid">'+id+'</span><span class="fval">'+(f.value!=null?f.value.toFixed(3):'N/A')+' (w:'+(f.weight!=null?f.weight.toFixed(2):'N/A')+')</span></div>'}).join('');
        var dh=p.delta_explanation?'<div class="dx">'+p.delta_explanation+'</div>':'';

        // Probability bar from key factors
        var homeProb=0,drawProb=0,awayProb=0;
        if(p.primary_ft){
          var parts=p.primary_ft.split('-');
          if(parts.length>=2){homeProb=parseFloat(parts[0])||0;awayProb=parseFloat(parts[1])||0;drawProb=Math.max(0,100-homeProb-awayProb)}
        }
        var probBar = homeProb>0||awayProb>0 ? '<div class="prob-bar"><div class="prob-h" style="width:'+homeProb+'%"></div><div class="prob-d" style="width:'+drawProb+'%"></div><div class="prob-a" style="width:'+awayProb+'%"></div></div><div class="prob-labels"><span>主 '+homeProb+'%</span><span>平 '+drawProb+'%</span><span>客 '+awayProb+'%</span></div>' : '';

        return '<div class="pc '+lk+' '+fd+'">'+
          '<div class="ph"><div class="pm">'+p.match_id+'</div><div class="pv '+vc+'">'+p.version_tag+'</div></div>'+
          '<div class="pd">'+(p.direction_judgment||'N/A')+'</div>'+
          probBar+
          '<div class="ps">'+
            '<div class="sbox"><div class="sl">主推 ('+pt+')</div><div class="sv">'+(p.primary_ft||'N/A')+' <span class="ht">('+(p.primary_ht||'-')+')</span></div></div>'+
            '<div class="sbox"><div class="sl">备选 ('+ht+')</div><div class="sv">'+(p.hedge_ft||'N/A')+' <span class="ht">('+(p.hedge_ht||'-')+')</span></div></div>'+
          '</div>'+
          '<div class="pf">'+ab+'<span>'+(p.is_lock?'LOCKED':'')+'</span></div>'+
          dh+(fh?'<details><summary>Key Factors ('+Object.keys(p.key_factors||{}).length+')</summary><ul class="fl">'+fh+'</ul></details>':'')+
        '</div>';
      }).join('');
      document.getElementById('predictions-container').innerHTML=h;
    }

    function renderOdds(ss){
      var h='<div class="card"><div class="ct">赔率快照 ('+ss.length+')</div><table class="dt2"><thead><tr><th>时间</th><th>主</th><th>平</th><th>客</th><th>信号</th></tr></thead><tbody>'+
        ss.map(function(s){
          var sig=s.is_steam_move?'<span class="tm">STEAM</span>':s.is_sharp_move?'<span class="ts">SHARP</span>':'-';
          return '<tr><td>'+new Date(s.captured_at).toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit'})+'</td><td>'+s.home_odds+'</td><td>'+s.draw_odds+'</td><td>'+s.away_odds+'</td><td>'+sig+'</td></tr>';
        }).join('')+
        '</tbody></table></div>';
      document.getElementById('predictions-container').insertAdjacentHTML('beforeend',h);
    }

    async function loadReviews(){
      try{
        var r=await fetch(API_BASE+'/api/reviews?limit=20');var d=await r.json();var rv=d.reviews||[];
        document.getElementById('stat-total').textContent=rv.length;
        document.getElementById('stat-hitrate').textContent=rv.length>0?((rv.filter(function(r){return r.error_type==='none'}).length/rv.length)*100).toFixed(0)+'%':'0%';
        document.getElementById('stat-upsets').textContent=rv.filter(function(r){return r.is_upset}).length;
        if(rv.length===0){document.getElementById('reviews-container').innerHTML='<div class="es"><div class="esi">&#128202;</div><div class="est">暂无复盘数据</div></div>';return}
        document.getElementById('reviews-container').innerHTML=rv.map(function(r){
          var at=r.actual_result==='home_win'?'主胜':r.actual_result==='draw'?'平局':'客胜';
          var errBadge=r.error_type==='none'?'<span class="bdg ba">CORRECT</span>':'<span class="bdg bf">'+r.error_type.toUpperCase()+'</span>';
          return '<div class="card">'+
            '<div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px">'+
              '<div><strong style="font-size:14px">'+r.match_id+'</strong><br><span class="bdg bp" style="margin-top:4px;display:inline-block">'+r.attribution_code+'</span></div>'+
              '<div style="font-size:11px;color:var(--text-dim);text-align:right">'+(r.created_at?new Date(r.created_at).toLocaleDateString('zh-CN'):'')+'<br>'+errBadge+'</div>'+
            '</div>'+
            '<div style="margin-top:10px;font-size:13px;line-height:1.6">'+r.attribution_detail+'</div>'+
            '<div style="margin-top:8px;font-size:12px;color:var(--text-dim)">实际: <strong style="color:var(--text)">'+at+' '+r.actual_ft+'</strong> | 归因: <strong>'+r.attribution_party+'</strong>'+(r.is_upset?' | <span style="color:var(--danger);font-weight:700">冷门</span>':'')+'</div>'+
          '</div>';
        }).join('');
        document.getElementById('reviews-container').dataset.loaded='true';
      }catch(e){document.getElementById('reviews-container').innerHTML='<div class="es"><div class="est">加载失败: '+e.message+'</div></div>'}
    }

    async function loadAllMatchFacts(){
      try{
        var r=await fetch(API_BASE+'/api/all-match-facts');var d=await r.json();var ms=d.matches||[];
        if(ms.length===0){document.getElementById('matches-container').innerHTML='<div class="es"><div class="esi">&#128202;</div><div class="est">暂无基本面数据</div></div>';return}
        document.getElementById('matches-container').innerHTML=ms.map(function(m){
          var confColor=m.data_confidence>0.7?'var(--success)':m.data_confidence>0.4?'var(--warning)':'var(--danger)';
          return '<div class="card">'+
            '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">'+
              '<div style="font-size:14px;font-weight:700">'+m.match_id+'</div>'+
              '<span class="bdg bp">'+m.status+'</span>'+
            '</div>'+
            '<table class="dt2"><thead><tr><th>指标</th><th>主队</th><th>客队</th></tr></thead><tbody>'+
              '<tr><td>xG</td><td>'+(m.home_xg_adj?m.home_xg_adj.toFixed(2):'-')+'</td><td>'+(m.away_xg_adj?m.away_xg_adj.toFixed(2):'-')+'</td></tr>'+
              '<tr><td>失球率</td><td>'+(m.home_conc_adj?m.home_conc_adj.toFixed(2):'-')+'</td><td>'+(m.away_conc_adj?m.away_conc_adj.toFixed(2):'-')+'</td></tr>'+
              '<tr><td>伤停</td><td>'+(m.injury_impact_home?m.injury_impact_home.toFixed(2):'-')+'</td><td>'+(m.injury_impact_away?m.injury_impact_away.toFixed(2):'-')+'</td></tr>'+
              '<tr><td>战意</td><td>'+(m.motivation_home?m.motivation_home.toFixed(2):'-')+'</td><td>'+(m.motivation_away?m.motivation_away.toFixed(2):'-')+'</td></tr>'+
            '</tbody></table>'+
            '<div style="margin-top:10px;font-size:11px;color:var(--text-dim);line-height:1.6">'+
              '天气: '+(m.weather_decay?m.weather_decay.toFixed(2):'-')+' | 裁判: '+m.referee_strictness+' | 赔率区间: '+(m.odds_zone||'-')+
              '<br>置信度: <strong style="color:'+confColor+'">'+(m.data_confidence?(m.data_confidence*100).toFixed(0)+'%':'-')+'</strong>'+
            '</div>'+
          '</div>';
        }).join('');
        document.getElementById('matches-container').dataset.loaded='true';
      }catch(e){document.getElementById('matches-container').innerHTML='<div class="es"><div class="est">加载失败: '+e.message+'</div></div>'}
    }

    async function loadConfig(){
      try{
        var r=await fetch(API_BASE+'/api/config?key=factor_weights');var d=await r.json();var w=d.value||{};
        var e1=Object.entries(w);
        document.getElementById('config-container').innerHTML=e1.length>0?e1.map(function(r){return '<div class="fi2"><span class="fid">'+r[0]+'</span><span class="fval">'+r[1]+'</span></div>'}).join(''):'<div class="es"><div class="est">无配置数据</div></div>';
      }catch(e){document.getElementById('config-container').innerHTML='<div class="es"><div class="est">加载失败: '+e.message+'</div></div>'}
      try{
        var r2=await fetch(API_BASE+'/api/config?key=review_config');var d2=await r2.json();var c=d2.value||{};
        var e2=Object.entries(c);
        document.getElementById('review-config-container').innerHTML=e2.length>0?e2.map(function(r){return '<div class="fi2"><span class="fid">'+r[0]+'</span><span class="fval">'+r[1]+'</span></div>'}).join(''):'<div class="es"><div class="est">无配置数据</div></div>';
      }catch(e){document.getElementById('review-config-container').innerHTML='<div class="es"><div class="est">加载失败: '+e.message+'</div></div>'}
    }

    async function loadUpcomingMatches(){
      try{
        var r=await fetch(API_BASE+'/api/upcoming-matches');var d=await r.json();var ms=d.matches||[];
        if(ms.length===0){document.getElementById('fixtures-container').innerHTML='<div class="es"><div class="esi">&#128197;</div><div class="est">暂无赛程数据<br><span style="font-size:11px;color:var(--text-muted)">Cron 将自动拉取</span></div></div>';return}
        document.getElementById('fixtures-container').innerHTML=ms.map(function(m){
          var ko=m.kickoff_time?new Date(m.kickoff_time).toLocaleString('zh-CN',{timeZone:'Asia/Shanghai',month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}):'TBD';
          return '<div class="fc"><div class="ft"><div><div class="fte">'+(m.home_team||'TBD')+' <span style="color:var(--text-dim);font-weight:400">vs</span> '+(m.away_team||'TBD')+'</div><div class="ftl">'+(m.league||'')+(m.round?' | R'+m.round:'')+'</div></div><div class="fti">'+ko+'</div></div><div class="fm">'+m.match_id+(m.venue?' | '+m.venue:'')+' | '+m.status+'</div></div>';
        }).join('');
        document.getElementById('fixtures-container').dataset.loaded='true';
      }catch(e){document.getElementById('fixtures-container').innerHTML='<div class="es"><div class="est">加载失败: '+e.message+'</div></div>'}
    }

    async function loadBettingWindowConfig(){
      try{
        var r=await fetch(API_BASE+'/api/config/betting-window');var d=await r.json();var c=d.config||{};
        document.getElementById('bw-start').value=c.start_hours_before_kickoff!=null?c.start_hours_before_kickoff:2;
        document.getElementById('bw-end').value=c.end_minutes_before_kickoff!=null?c.end_minutes_before_kickoff:15;
        document.getElementById('bw-fund-delay').value=c.fundamentals_delay_after_start_hours!=null?c.fundamentals_delay_after_start_hours:0.5;
        document.getElementById('bw-final-lock').value=c.final_lock_minutes_before_end!=null?c.final_lock_minutes_before_end:15;
        document.getElementById('bw-season').value=c.season||'2025';
        document.getElementById('bw-status').textContent='已加载 | 联赛: '+(c.api_football_league_ids||[39,140,135,78,61]).join(', ');
        document.getElementById('bw-status').dataset.loaded='true';
      }catch(e){document.getElementById('bw-status').textContent='加载失败: '+e.message}
    }

    async function saveBettingWindowConfig(){
      var s=parseInt(document.getElementById('bw-start').value),
          e=parseInt(document.getElementById('bw-end').value),
          fd=parseFloat(document.getElementById('bw-fund-delay').value),
          fl=parseInt(document.getElementById('bw-final-lock').value),
          sn=document.getElementById('bw-season').value.trim();
      if(!s||s<1||s>72){alert('开始购彩时间须为 1-72 小时');return}
      if(isNaN(e)||e<0||e>120){alert('截止购彩时间须为 0-120 分钟');return}
      if(e>=s*60){alert('截止购彩时间须早于开始购彩时间');return}
      if(isNaN(fd)||fd<0||fd>6){alert('基本面延迟须为 0-6 小时');return}
      if(isNaN(fl)||fl<1||fl>60){alert('终盘锁定须为 1-60 分钟');return}
      document.getElementById('bw-status').textContent='保存中...';
      try{
        var r=await fetch(API_BASE+'/api/config/betting-window',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({
          start_hours_before_kickoff:s,
          end_minutes_before_kickoff:e,
          fundamentals_delay_after_start_hours:fd,
          final_lock_minutes_before_end:fl,
          league_ids:[39,140,135,78,61],
          season:sn||'2025'
        })});
        var d=await r.json();
        if(d.success){document.getElementById('bw-status').innerHTML='<span style="color:var(--success)">保存成功</span> | 赛前'+s+'h / 赛前'+e+'min | 延迟'+fd+'h / 锁定'+fl+'min'}
        else{document.getElementById('bw-status').innerHTML='<span style="color:var(--danger)">失败: '+(d.error||'未知错误')+'</span>'}
      }catch(e){document.getElementById('bw-status').innerHTML='<span style="color:var(--danger)">保存失败: '+e.message+'</span>'}
    }

    checkHealth();
    setInterval(checkHealth,60000);
  </script>
</body>
</html>
`;
}

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    const path = url.pathname;

    if (request.method === "OPTIONS") {
      return new Response(null, { headers: CORS_HEADERS });
    }

    // API Routes
    if (path === "/api/health") {
      try {
        const data = await supabaseGet("match_facts", {}, "match_id");
        return jsonResponse({
          status: "healthy",
          database: Array.isArray(data) ? "ok" : "error",
          timestamp: new Date().toISOString(),
        });
      } catch (e) {
        return jsonResponse({ status: "error", database: "error", message: e.message });
      }
    }

    if (path === "/api/predictions") {
      const matchId = url.searchParams.get("matchId");
      if (!matchId) {
        return jsonResponse({ error: "matchId required" }, 400);
      }
      const data = await supabaseGet(
        "predictions",
        { match_id: "eq." + matchId, order: "created_at.desc", limit: "10" },
        "id,match_id,primary_result,primary_ft,primary_ht,hedge_result,hedge_ft,hedge_ht,direction_judgment,version_tag,is_lock,alignment_status,alignment_forced_degrade,key_factors,delta_explanation,created_at"
      );
      if (!data || data.length === 0) {
        return jsonResponse({ prediction: null });
      }
      const latest = data.find((p) => !p.is_archived) || data[0];
      return jsonResponse({ prediction: latest });
    }

    if (path === "/api/prediction") {
      const matchId = url.searchParams.get("matchId");
      if (!matchId) {
        return jsonResponse({ error: "matchId required" }, 400);
      }
      const data = await supabaseGet(
        "predictions",
        { match_id: "eq." + matchId, is_lock: "eq.true", order: "created_at.desc", limit: "1" },
        "id,match_id,primary_result,primary_ft,primary_ht,hedge_result,hedge_ft,hedge_ht,direction_judgment,version_tag,is_lock,alignment_status,alignment_forced_degrade,key_factors,delta_explanation,created_at"
      );
      if (!data || data.length === 0) {
        const fallback = await supabaseGet(
          "predictions",
          { match_id: "eq." + matchId, order: "created_at.desc", limit: "1" },
          "id,match_id,primary_result,primary_ft,primary_ht,hedge_result,hedge_ft,hedge_ht,direction_judgment,version_tag,is_lock,alignment_status,alignment_forced_degrade,key_factors,delta_explanation,created_at"
        );
        if (!fallback || fallback.length === 0) {
          return jsonResponse({ error: "No locked prediction found" }, 404);
        }
        return jsonResponse({ prediction: fallback[0] });
      }
      return jsonResponse({ prediction: data[0] });
    }

    if (path === "/api/all-predictions") {
      const data = await supabaseGet(
        "predictions",
        { is_archived: "eq.false", order: "created_at.desc", limit: "50" },
        "id,match_id,primary_result,primary_ft,primary_ht,hedge_result,hedge_ft,hedge_ht,direction_judgment,version_tag,is_lock,alignment_status,alignment_forced_degrade,key_factors,delta_explanation,created_at"
      );
      return jsonResponse({ predictions: data || [] });
    }

    if (path === "/api/match-facts") {
      const matchId = url.searchParams.get("matchId");
      if (!matchId) {
        return jsonResponse({ error: "matchId required" }, 400);
      }
      const data = await supabaseGet("match_facts", { match_id: "eq." + matchId }, "*");
      return jsonResponse({ facts: data[0] || null });
    }

    if (path === "/api/odds-snapshots") {
      const matchId = url.searchParams.get("matchId");
      if (!matchId) {
        return jsonResponse({ error: "matchId required" }, 400);
      }
      const data = await supabaseGet(
        "odds_snapshots",
        { match_id: "eq." + matchId, order: "captured_at.desc", limit: "10" },
        "id,match_id,captured_at,home_odds,draw_odds,away_odds,source,signal_type,is_sharp_move,is_steam_move"
      );
      return jsonResponse({ snapshots: data || [] });
    }

    if (path === "/api/reviews") {
      const limit = url.searchParams.get("limit") || "20";
      const matchId = url.searchParams.get("matchId");
      const query = { order: "created_at.desc", limit };
      if (matchId) { query.match_id = "eq." + matchId; }
      const data = await supabaseGet(
        "review_results",
        query,
        "id,match_id,prediction_id,actual_result,actual_ft,actual_ht,attribution_code,attribution_party,attribution_detail,error_type,is_upset,created_at"
      );
      return jsonResponse({ reviews: data || [] });
    }

    if (path === "/api/config") {
      const key = url.searchParams.get("key") || "factor_weights";
      const data = await supabaseGet("system_config", { key: "eq." + key }, "key,value");
      return jsonResponse({ key, value: data[0] ? data[0].value : {} });
    }

    if (path === "/api/config/betting-window") {
      if (request.method === "GET") {
        const data = await supabaseGet("system_config", { key: "eq.betting_window_config" }, "key,value");
        return jsonResponse({ config: data[0] ? data[0].value : {} });
      }
      if (request.method === "POST") {
        try {
          const body = await request.json();
          const start = body.start_hours_before_kickoff;
          const end = body.end_minutes_before_kickoff;
          const fundDelay = body.fundamentals_delay_after_start_hours;
          const finalLock = body.final_lock_minutes_before_end;
          const league_ids = body.league_ids;
          const season = body.season;
          if (typeof start !== "number" || start < 1 || start > 72) {
            return jsonResponse({ error: "start_hours_before_kickoff must be 1-72" }, 400);
          }
          if (typeof end !== "number" || end < 0 || end > 120) {
            return jsonResponse({ error: "end_minutes_before_kickoff must be 0-120" }, 400);
          }
          if (end >= start * 60) {
            return jsonResponse({ error: "end must be less than start * 60" }, 400);
          }
          const cfg = {
            start_hours_before_kickoff: start,
            end_minutes_before_kickoff: end,
            fundamentals_delay_after_start_hours: typeof fundDelay === "number" ? fundDelay : 0.5,
            final_lock_minutes_before_end: typeof finalLock === "number" ? finalLock : 15,
            api_football_league_ids: Array.isArray(league_ids) ? league_ids : [39, 140, 135, 78, 61],
            target_leagues: ["soccer_epl", "soccer_spain_la_liga", "soccer_italy_serie_a", "soccer_germany_bundesliga", "soccer_france_ligue_one"],
            season: typeof season === "string" ? season : "2025",
          };
          const result = await supabasePost("system_config", { key: "betting_window_config", value: cfg });
          return jsonResponse({ success: true, config: cfg, result });
        } catch (e) {
          return jsonResponse({ error: e.message }, 500);
        }
      }
    }

    if (path === "/api/upcoming-matches") {
      const data = await supabaseGet(
        "matches",
        { status: "eq.scheduled", order: "kickoff_time.asc", limit: "20" },
        "match_id,home_team,away_team,league,kickoff_time,status,round,venue"
      );
      return jsonResponse({ matches: data || [] });
    }

    if (path === "/api/all-match-facts") {
      const data = await supabaseGet(
        "match_facts",
        { order: "match_id.asc" },
        "match_id,home_xg_adj,away_xg_adj,home_conc_adj,away_conc_adj,injury_impact_home,injury_impact_away,weather_decay,referee_strictness,motivation_home,motivation_away,odds_zone,bias_correction,data_confidence,status,created_at"
      );
      return jsonResponse({ matches: data || [] });
    }

    // Serve HTML for root and non-API paths
    if (path === "/" || path === "/index.html" || !path.startsWith("/api/")) {
      const html = getHTML();
      return new Response(html, {
        headers: { "Content-Type": "text/html; charset=utf-8", "Cache-Control": "public, max-age=0, must-revalidate" },
      });
    }

    return jsonResponse({ error: "Not found" }, 404);
  },
};
